use crate::CharacterSheet;
use super::Career;


pub fn select_new_carreer(sheet: &mut CharacterSheet, careers : &mut Vec<Career> ) -> Option<Career>{

    //TODO:
    //print list of available carrers
    //allow selection
    //try to succeed
    //branch out if that does not work
    //remove selected carrer from available carrers and return selected carrer
    
    None
}